# ========================================
# CONFIGURAÇÃO PARA RENDER (PRODUÇÃO)
# ========================================

# Database - Usar variável de ambiente no Render
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d2fq6sndiees73co55f0-a.virginia-postgres.render.com:5432/proj10db?sslmode=require}
spring.datasource.username=${DB_USERNAME:proj10}
spring.datasource.password=${DB_PASSWORD:DZ1JqenYqhrsSY8rwPPUqUhotac3FeoN}
spring.datasource.driver-class-name=org.postgresql.Driver

# Server - Porta dinâmica no Render
server.port=${PORT:8080}
server.address=0.0.0.0

# Hibernate/JPA - Configuração para produção
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:false}

# Pool de conexões HikariCP - Otimizado para Render
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=${MAX_POOL_SIZE:5}
spring.datasource.hikari.minimum-idle=${MIN_IDLE:1}
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# CORS - Permitir todas as origens (ajustar depois)
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,HEAD
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Logging - Menos verboso em produção
logging.level.org.hibernate.SQL=${LOG_SQL:WARN}
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=${LOG_BINDER:WARN}
logging.level.com.zaxxer.hikari=${LOG_HIKARI:INFO}
logging.level.org.springframework.web=${LOG_WEB:INFO}

# Security - Desabilitado
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# SSL
server.ssl.enabled=false

# JPA
spring.jpa.open-in-view=falsegit add .
git commit -m "feat: configuração para deploy no Render"
git push origin main