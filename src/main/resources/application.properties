# ========================================
# CONFIGURAÇÃO PARA RENDER (PRODUÇÃO)
# ========================================

# Converter DATABASE_URL automaticamente
spring.datasource.url=jdbc:postgresql://dpg-d2fq6sndiees73co55f0-a.virginia-postgres.render.com:5432/proj10db?sslmode=require
spring.datasource.username=proj10
spring.datasource.password=DZ1JqenYqhrsSY8rwPPUqUhotac3FeoN

# Server - Porta dinâmica no Render
server.port=${PORT:8080}

# Hibernate/JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:false}

# Pool de conexões
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=${MAX_POOL_SIZE:3}
spring.datasource.hikari.minimum-idle=${MIN_IDLE:1}
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# Logging
logging.level.org.hibernate.SQL=${LOG_SQL:WARN}
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.com.zaxxer.hikari=INFO
logging.level.org.springframework.web=INFO

# SSL
server.ssl.enabled=false

# JPA
spring.jpa.open-in-view=false

# CORS (configure conforme seu frontend)
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*